/* ==========================================
   THÈME GLOBAL
========================================== */
body {
  font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Arial, sans-serif;
  background-color: #1f2329;
  color: #e1e4e8;
  line-height: 1.75;
  margin: 0;
}

/* Conteneur principal */
.container,
.page-header,
main {
  max-width: 860px;
  margin: 0 auto;
  padding: 0 10px;
}

/* Mode clair */
body.light {
  background-color: #ffffff;
  color: #000000;
}

/* ==========================================
   ACCESSIBILITÉ
========================================== */
#accessibility-menu {
  position: fixed;
  top: 10px;
  right: 10px;
  background: #2a3038;
  padding: 8px;
  border-radius: 8px;
  display: flex;
  gap: 6px;
  z-index: 999;
}

#accessibility-menu button {
  background: #4c78a8;
  border: none;
  color: white;
  padding: 6px 8px;
  border-radius: 4px;
  cursor: pointer;
}

#accessibility-menu button:hover {
  background: #79b8ff;
}

/* ==========================================
   HEADER
========================================== */
.page-header {
  margin-top: 15px;
}

/* Navigation */
.top-nav {
  margin-bottom: 15px;
  font-size: 1rem;
}

.top-nav a {
  font-weight: 500;
  color: #79b8ff;
  text-decoration: none;
}

.top-nav span {
  margin: 0 5px;
}

body.light .top-nav a {
  color: #0056b3;
}

/* Menu hyperlien*/
.menu-programme {
  background: #2a3038;
  padding: 15px;
  border-radius: 6px;
  margin: 20px 0;
}

/* Mode clair : menu accès rapide */

body.light .menu-programme {
  background: #ffffff;
  border: 1px solid #d0d7de;
  box-shadow: 0 2px 6px rgba(0,0,0,0.05);
}


body.light .menu-programme a {
  color: #0056b3;        /* bleu lisible */
}

body.light .menu-programme a:hover {
  color: #003366;
}

.menu-programme h2 {
  margin-top: 0;
}

.menu-programme ul {
  list-style: none;
  padding-left: 0;
}

.menu-programme li {
  margin: 5px 0;
}

.menu-programme ul ul {
  margin-left: 15px;
}

html {
  scroll-behavior: smooth;
}


/* ==========================================
   BANDEAU
========================================== */

.top-banner {
  text-align: center;
  padding: 12px 10px;            /* moins épais */
  border-radius: 4px;            /* plus discret */

  background-color: #2f4254;     /* un peu moins saturé */
  color: #ffffff;

  margin-top: 8px;
}


/* Ligne principale */
.banner-main {
  display: block;
  font-size: 1.6rem;
  font-weight: 700;
}

/* Sous-titre */
.banner-sub {
  display: block;
  font-size: 1rem;
  margin-top: 4px;
  opacity: 0.8;
}

/* Mode clair */
body.light .top-banner {
  background-color: #e9eef3;
  color: #1f2329;
}

/* ==========================================
   TITRES
========================================== */
h1 {
  color: #eaeef2;
  font-size: 2.4em;
  margin-bottom: 30px;
  padding-bottom: 10px;
  border-bottom: 2px solid #3a3f45;
}

/* Premier titre principal */
h1:first-of-type {
  margin-top: 40px;
  font-weight: 700;
}

/* Mode clair corrigé */
body.light h1 {
  color: #2c2c2c;
}

body.light h1:first-of-type {
  color: #1a1a1a;
}

/* H2 */
h2 {
  color: #b8d7ff;
  font-size: 1.6em;
  margin-top: 20px;
  margin-bottom: 12px;
  padding-left: 12px;
  border-left: 4px solid #4c78a8;
}

/* H3 */
h3 {
  color: #9ecb8f;
  font-size: 1.25em;
  margin-top: 20px;
  margin-bottom: 12px;
  text-transform: uppercase;
}

/* H4 */
h4 {
  color: #d0b080;
  font-size: 1.05em;
  margin-top: 20px;
  margin-bottom: 12px;
}

/* Mode clair titres */
body.light h2 {
  color: #003366;
  border-left-color: #003366;
}

body.light h3 {
  color: #1f7a1f;
}

body.light h4 {
  color: #7a4a00;
}

/* ==========================================
   TEXTE
========================================== */
p {
  margin: 18px 0;
  font-size: 1.05em;
}

/* Intro */
.intro {
  font-size: 1.1rem;
  margin: 25px 0;
  line-height: 1.6;
}

body.light .intro {
  color: #333;
}

/* Emphase */
em {
  color: #f2b6c6;
  font-style: normal;
  font-weight: bold;
}

body.light em {
  color: #c2185b;
}

/* ==========================================
   LIENS
========================================== */
a {
  color: #79b8ff;
  text-decoration: none;
  border-bottom: 1px solid rgba(121, 184, 255, 0.3);
}

a:hover {
  color: #ffffff;
  border-bottom-color: #ffffff;
}

body.light a {
  color: #0056b3;
}

/* ==========================================
   BLOCS
========================================== */
blockquote {
  background-color: #2a3038;
  border-left: 4px solid #79b8ff;
  padding: 18px 22px;
  margin: 30px 0;
  color: #cdd9e5;
}

code {
  background-color: #2d333b;
  color: #c9d1d9;
  padding: 3px 6px;
  border-radius: 4px;
  font-family: "Fira Code", monospace;
}

hr {
  border: none;
  border-top: 1px solid #3a3f45;
  margin: 40px 0;
}

/* ==========================================
   TABLEAUX
========================================== */
table { 
  width: auto;           /* ✅ au lieu de 100% */
  border-collapse: collapse;
  table-layout: auto;
}


col.col-min {
  width: 1%;
}

td:first-child {
  white-space: nowrap;
}


table th,
table td {
  border: 1px solid #444;
  padding: 8px;
}

/* ==========================================
   RESPONSIVE
========================================== */
@media (max-width: 600px) {

  .banner-main {
    font-size: 1.3rem;
  }

  .banner-sub {
    font-size: 1.1rem;
  }

  .top-banner {
    padding: 14px 8px;
  }

  .intro {
    font-size: 1rem;
  }
}

/* ==========================================
   IMPRESSION
========================================== */
@media print {
  body {
    background-color: #ffffff;
    color: #000000;
  }
}